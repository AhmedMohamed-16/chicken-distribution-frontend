import { CurrencyArabicPipe } from './currency-arabic-pipe';

describe('CurrencyArabicPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyArabicPipe();
    expect(pipe).toBeTruthy();
  });
});
