import { StatusArabicPipe } from './status-arabic-pipe';

describe('StatusArabicPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusArabicPipe();
    expect(pipe).toBeTruthy();
  });
});
